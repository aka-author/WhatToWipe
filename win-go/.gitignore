# Only artifacts dropped in the module root вЂ” not dist/ (bundle lives there).
/Trash Advisor.exe
/Trash Advisor.config.txt
# Ignore the binary in dist/ only; keep Trash Advisor.config.txt visible next to the exe.
dist/*.exe
