# Only artifacts dropped in the module root РІР‚вЂќ not dist/ (bundle lives there).
/Erase & Rewrite.exe
/Erase & Rewrite.config.txt
# Ignore the binary in dist/ only; keep Erase & Rewrite.config.txt visible next to the exe.
dist/*.exe
